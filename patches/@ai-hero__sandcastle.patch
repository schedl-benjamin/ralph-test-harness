diff --git a/dist/SandboxFactory.js b/dist/SandboxFactory.js
index f96bb2dcf07d71a446777f5954fa25e8e117c0f9..8f44cafa06e5e7599bd36419357b1c8b649fac9d 100644
--- a/dist/SandboxFactory.js
+++ b/dist/SandboxFactory.js
@@ -176,9 +176,13 @@ export const WorktreeDockerSandboxFactory = {
                     : Effect.succeed(undefined)).pipe(Effect.map(() => worktreeInfo))))
                     .pipe(Effect.flatMap((worktreeInfo) => {
                     const gitDir = join(hostRepoDir, ".git");
+                    // Convert Windows paths to Docker-compatible format (C:\foo → /c/foo)
+                    const toDockerVolume = (p) => process.platform === "win32"
+                        ? "/" + p.replace(/\\/g, "/").replace(/^([A-Za-z]):/, (_, d) => d.toLowerCase())
+                        : p;
                     const volumeMounts = [
-                        `${worktreeInfo.path}:${SANDBOX_WORKSPACE_DIR}`,
-                        `${gitDir}:${gitDir}`,
+                        `${toDockerVolume(worktreeInfo.path)}:${SANDBOX_WORKSPACE_DIR}`,
+                        `${toDockerVolume(gitDir)}:${toDockerVolume(gitDir)}`,
                     ];
                     // On signals: remove the container but preserve the worktree so the
                     // developer can inspect the agent's work.
@@ -196,7 +200,11 @@ export const WorktreeDockerSandboxFactory = {
                         volumeMounts,
                         workdir: SANDBOX_WORKSPACE_DIR,
                         user: `${hostUid}:${hostGid}`,
-                    }).pipe(Effect.andThen(chownInContainer(containerName, `${hostUid}:${hostGid}`, "/home/agent")), Effect.tap(() => Effect.sync(() => {
+                    }).pipe(Effect.andThen(chownInContainer(containerName, `${hostUid}:${hostGid}`, "/home/agent")),
+                    // Also chown the host .git dir mount so the container user can write git objects.
+                    // On Docker Desktop for Windows, bind-mounted NTFS dirs appear as root-owned.
+                    Effect.andThen(chownInContainer(containerName, `${hostUid}:${hostGid}`, toDockerVolume(gitDir))),
+                    Effect.tap(() => Effect.sync(() => {
                         // exit handler only removes the container; the worktree is preserved
                         // on abnormal exit just as on SIGINT/SIGTERM.
                         process.on("exit", cleanupContainerOnly);
diff --git a/dist/SandboxLifecycle.js b/dist/SandboxLifecycle.js
index 664fff77f22b1ba44a1bc7eb85ba2827f37eff57..bc3a74e2572924e355d8e29c9d128501102433ef 100644
--- a/dist/SandboxLifecycle.js
+++ b/dist/SandboxLifecycle.js
@@ -81,7 +81,10 @@ export const withSandboxLifecycle = (options, work) => Effect.gen(function* () {
         // Check if there are any new commits on the temp branch
         const hasNewCommits = yield* Effect.promise(async () => {
             try {
-                const { stdout } = await execAsync(`git rev-list "${baseHead}..HEAD" --count`, { cwd: hostSideWorktreePath });
+                // Use branch ref from hostRepoDir instead of checking HEAD in worktree.
+                // On Windows, the worktree .git file may have MSYS paths (/c/dev/...)
+                // set by entrypoint.sh, causing host-side git to fail.
+                const { stdout } = await execAsync(`git rev-list "${baseHead}..refs/heads/${resolvedBranch}" --count`, { cwd: hostRepoDir });
                 return parseInt(stdout.trim(), 10) > 0;
             }
             catch {
diff --git a/dist/WorktreeManager.js b/dist/WorktreeManager.js
index 22b221ec840476a04a822fa8fa551642fe2f4b54..8cada32c0e925c1580f9ae7ddd3d23cc00b51e8a 100644
--- a/dist/WorktreeManager.js
+++ b/dist/WorktreeManager.js
@@ -1,6 +1,7 @@
 import { Effect, Option } from "effect";
 import { FileSystem } from "@effect/platform";
 import { execFile } from "node:child_process";
+import { rmSync } from "node:fs";
 import { join } from "node:path";
 import { WorktreeError } from "./errors.js";
 /** Format a timestamp as YYYYMMDD-HHMMSS */
@@ -138,7 +139,11 @@ export const hasUncommittedChanges = (worktreePath) => execGit(["status", "--por
 export const remove = (worktreePath) => {
     // Derive the main repo dir: worktreePath = <repoDir>/.sandcastle/worktrees/<name>
     const repoDir = join(worktreePath, "..", "..", "..");
-    return execGit(["worktree", "remove", "--force", worktreePath], repoDir).pipe(Effect.asVoid);
+    return execGit(["worktree", "remove", "--force", worktreePath], repoDir).pipe(Effect.catchAll(() => {
+        // Fallback for Windows where git worktree remove fails with ENOSYS/EPERM
+        try { rmSync(worktreePath, { recursive: true, force: true }); } catch {}
+        return execGit(["worktree", "prune"], repoDir);
+    }), Effect.asVoid);
 };
 /**
  * Prunes stale git worktree metadata and removes orphaned directories under
